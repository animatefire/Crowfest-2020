const Home = {
  render: () => {
    const markup = `
    <section id="home">
      <section class="content-right">
          <button>Tickets</button>
          <video></video>
      </section>
    </section>
    `;
    return markup;
  }
};

export default Home;
